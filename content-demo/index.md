# Demo Content

This is intentionally tiny. It exists only to prove that the app shell can ingest Markdown, serve nodes, search, open a detail page, link a quiz to a node, and support browser editing.

Real notes, quiz banks, source traces, and `knowledge.db` belong in a private data directory such as `../cs-learning-data/`.
